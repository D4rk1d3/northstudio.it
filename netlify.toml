[build]
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"
  included_files = ["netlify/functions/package.json"]

# Blocca accesso diretto alla pagina admin tramite redirect a se stessa (opzionale, extra layer)
# La vera protezione è la Netlify Function verify-admin
